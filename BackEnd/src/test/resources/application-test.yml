spring:
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: none
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://dummy.invalid

app:
  security:
    jwt:
      enabled: false