# Long-Term Memory Folder Instructions

This folder is reserved for Markdown (`.md`) files used as long-term memory.

Rule:
- Do not modify any code unless the user explicitly says `코드수정해줘`.
- All new frontend development must use TypeScript. Prefer `.ts` and `.tsx`, and do not add new `.js` or `.jsx` source files.
