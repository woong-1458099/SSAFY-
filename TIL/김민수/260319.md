# 2026-03-19 (목)

## 오늘 한 일
- nginx를 `core-net`뿐 아니라 `ops-net`에도 연결해 운영 도구 라우팅이 가능하도록 정리
- `dev.ssafymaker.cloud`, `grafana.ssafymaker.cloud`, `rabbitmq.ssafymaker.cloud` 도메인 라우팅 추가
- 각 운영 도메인의 80 포트 요청을 HTTPS로 리다이렉트하도록 정리
- nginx `app.conf`를 다시 수정해 80 포트 블록은 리다이렉트 전용으로 단순화하는 버그픽스 반영
- frontend Jenkinsfile(STG/PROD)에 target branch 검증 가드를 추가
- develop/master가 아닌 대상 브랜치로 들어오는 MR에서는 파이프라인이 잘못 돌지 않도록 차단

## 정리
- 공용 ingress가 app 네트워크와 ops 네트워크를 함께 보게 하면서 운영 도구 도메인 확장 기반을 맞췄다.
- HTTP 요청은 HTTPS로 일관되게 넘기도록 정리해 라우팅 구성을 단순화했다.
- frontend 배포 파이프라인에 branch guard를 넣어 불필요하거나 잘못된 배포 실행을 막을 수 있게 했다.

