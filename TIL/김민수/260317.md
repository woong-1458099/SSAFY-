# 2026-03-17 (화)

## 오늘 한 일
- backend Jenkinsfile(STG/PROD)을 수정해 배포 실패 시 target color 컨테이너를 정리하도록 보완
- `docker/compose.app.yml`에 `APP_PUBLIC_BASE_URL`, `APP_FRONTEND_BASE_URL` 환경변수 전달 추가
- `AppUrlProperties` 설정 바인딩 충돌을 줄이기 위해 중복 컴포넌트 등록 요소 정리
- `tunnel-206.bat`에 Grafana 점검용 로컬 포트 포워딩(`13000 -> 3000`) 추가
- backend / frontend README 및 구조 문서, MR 리뷰 라우팅 문서 정리

## 정리
- 배포 실패 뒤 남는 target color 컨테이너까지 정리되도록 Jenkins 후처리를 보완해 배포 안정성을 높였다.
- 인증/URL 관련 설정값이 compose에서 backend로 정확히 전달되도록 정리해 환경별 실행 차이를 줄였다.
- 운영 점검용 터널과 문서를 함께 정리하면서 이후 인프라 운영/리뷰 흐름을 더 보기 쉽게 맞췄다.

