### 2025-03-11
- GitLab webhook과 Jenkins trigger를 실제 MR/merge 흐름 기준으로 점검했다.
- 프론트 게임 화면에서 정적 에셋이 깨지는 문제를 분석했다.
- 게임 에셋 경로를 `public/assets/game` 기준으로 정리했다.
- Cloudflare cache purge 자동화를 Jenkins frontend pipeline에 추가했다.
- `current` 심볼릭 링크 마운트 방식이 nginx 컨테이너에서 새 릴리즈를 자동 반영하지 못하는 문제를 확인했다.
- 프론트 정적 파일 서빙 구조를 `live + rsync` 방식으로 전환했다.
- origin과 Cloudflare 경유 응답이 모두 `image/png` 기준으로 정상화되는 것까지 확인했다.